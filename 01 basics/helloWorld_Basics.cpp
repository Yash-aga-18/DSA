#include <bits/stdc++.h>
using namespace std;

int main()
{

//   cout<<"Hello World";
  
//  //   we can use endl or \n    to go to next line
  
//   cout<<"Hello World"<<endl;
//   cout<<"Hello meri duniya\n";
//   cout<<"Hello mere dosto\n";
   
  int x=10;
  cout<<(x=50)<<endl;
  cout<<x;
// here first the 50 is assigned to x and then print the value of x




  // c++ is camel casing type Camel Case
// Each word, except the first, starts with a capital letter
  
//   A variable name must start with a letter or the underscore character
// A variable name cannot start with a number
// A variable name can only contain alpha-numeric characters and underscores (A-z, 0-9, and _ )
// Variable names are case-sensitive (age, Age and AGE are three different variables) 
   

 // there are some other types of writing ba variable name like
 //camel case         myVariableName
 //pascal case        MyVariableName
 // snake case        my_variable_name


// if you define global variable a=9
// and in any function a=7   and int a=7 both have different meanings
// a=7 changes the global variable value of a from 9 to 7
// while int a = 7 have local scope



// float a = 20.34f; // Stored as a float
// double b = 20.34; // Stored as a double
// printf("%.10f\n", a); // May print: 20.3400001526 (less precision)
// printf("%.10lf\n", b); // May print: 20.3400000000 (more precision)



// we can calculate size occupied by any data type by using cout<<sizeof(int)

// In C++, all lines that start with a pound or hashtag # sign are called directives and
//  are processed by a preprocessor which is a program invoked by the compiler.


// #include<iostream> tells the preprocessor to include the iostream header file(file that has
// some already pre-written code which we are importing to avoid reinventing the wheel) in the
// program and iostream is the header file which contains all the 
// basic functions of the program like input/output etc.


  return 0 ;

}