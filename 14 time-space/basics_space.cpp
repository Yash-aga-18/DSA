#include <bits/stdc++.h>
using namespace std;

int main()
{
/*

   space complexity ------------------>
 
  O(1) means constant time — the algorithm takes the same amount of time regardless of the input size.
  O(k) == O(1)

  not valid to describe an infinite loop  
     

  O(kn) ==  O(n)
  O(n +- k) == O(n)
  O(n^k + n^m) = O(n^k)        // where k>m
  
  
  space complexity generally refers to the memory space that a code uses while being executed.
  Space complexity generally represents the sum of auxiliary and input space. 
  Auxiliary space refers to the space that we use additionally to solve a problem. 
  And input space refers to the space that we use to store the inputs
  
//  //--------------------------------------------------------------------------------------------------------------------------------------------------------------------

    int arr[n];
    
    for(int i=0; i<n; i++){
        cout<<i;
    }

    T.C is O(n)
    S.C is O(n)

  
//  //--------------------------------------------------------------------------------------------------------------------------------------------------------------------

    int c = 0;
    int s = 1;

    for(int i=0; i<n; i++){
        cout<<i;
    }
    
    T.C is O(n)
    S.C is O(1)


//  //--------------------------------------------------------------------------------------------------------------------------------------------------------------------

    int arr[m], brr[n], crr[x];


    for(int i=0; i<n; i++){
        cout<<i;
    
    }
    
    
    T.C is O(n)
    S.C is O(m+n+x)
    if all are of same size then   O(n+n+n) == O(3*n) == O(n)




    int arr[n], brr[n/2];

    for(int i=0; i<n; i++){
        cout<<i;
    
    }

    T.C is O(n)
    S.C is O(n)
    

//  //--------------------------------------------------------------------------------------------------------------------------------------------------------------------
    

    vector<int> a;
    vector<int> b;

    for(int i=0; i<n; i++){
       for(int j=0; j<m; j++){
            a.push_back(10);
            b.push_back(10);
        }
    }

    T.C is O(n*m)
    S.C is O(n*m)
    




    vector<int> a(n);
    vector<int> b(m);

    for(int i=0; i<n; i++){
       for(int j=0; j<m; j++){
            a.push_back(10);
            b.push_back(10);
        }
    }

    T.C is O(n*m)
    S.C is O(n+m)

    
//  //--------------------------------------------------------------------------------------------------------------------------------------------------------------------

int arr[n][m];

    for(int i=0; i<n; i++){
       for(int j=0; j<m; j++){
           cout<<arr[i][j]; 
        }
    }

        
    T.C is O(n*m)
    S.C is O(n*m)

//  //--------------------------------------------------------------------------------------------------------------------------------------------------------------------




*/ 
  
 return 0;

}